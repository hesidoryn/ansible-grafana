# password which you need to login to the grafana
gf_admin_password: paralect_drafana$
# api domain name used by telegraf
api_domain_name:
# ip address of server where influx is installed
influx_ip:
# influx database name
influx_db_name: "monitoring"
# mongo connection string, used by telegraf to monitor count of operations
mongo_connection:
# redis connection string, used by telegraf to monitor count of operations
# e.g.
# tcp://localhost:6379
# tcp://:password@192.168.99.100
# unix:///var/run/redis.sock
redis_connection:
